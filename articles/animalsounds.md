# How to get started with animalsounds

``` r

library(animalsounds)
```

The `animalsounds` package includes a simple function for combining an
animal name with the sound it makes.

``` r

animal_sounds("cat", "miau")
#> [1] "The cat goes miau!"
```
